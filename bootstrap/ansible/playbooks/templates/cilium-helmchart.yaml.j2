---
# https://docs.k3s.io/helm
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: cilium
  namespace: kube-system
spec:
  # renovate: datasource=helm
  repo: https://helm.cilium.io/
  chart: cilium
  version: 1.16.6
  targetNamespace: kube-system
  bootstrap: true
  timeout: 900s
  valuesContent: |-
{{ lookup('file', playbook_dir + '/../../../cluster/apps/kube-system/cilium/app/helm-values.yaml') | indent(4, first=True) }}
