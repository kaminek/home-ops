---
# https://docs.k3s.io/helm
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: coredns
  namespace: kube-system
spec:
  # renovate: datasource=helm
  repo: https://coredns.github.io/helm
  chart: coredns
  version: 1.39.2
  targetNamespace: kube-system
  bootstrap: true
  timeout: 900s
  valuesContent: |-
{{ lookup('file', playbook_dir + '/../../../cluster/apps/kube-system/coredns/app/helm-values.yaml') | indent(4, first=True) }}
