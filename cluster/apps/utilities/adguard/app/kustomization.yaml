---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./helm-release.yaml
configMapGenerator:
  - name: adguard-config
    files:
      - AdGuardHome.yaml=resources/config.yaml
generatorOptions:
  disableNameSuffixHash: true
